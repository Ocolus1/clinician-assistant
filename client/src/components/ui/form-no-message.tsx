import { FormMessage as ShadcnFormMessage } from "@/components/ui/form";

// This component extends the ShadcnFormMessage component but doesn't show any message
export function FormMessageHidden() {
  return null;
}